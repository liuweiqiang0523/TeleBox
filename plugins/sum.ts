// Official sum plugin is disabled in this fork.
// The customized SumPlus implementation lives in plugins/sumplus.ts and owns
// the .sum/.summary commands while reusing assets/sum/config.json.
export {};
